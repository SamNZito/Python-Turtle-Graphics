import turtle
from random import randint

turtle.speed(0)

turtle.colormode(255)

for x in range (400):
    
    r = randint(0, 255)
    g = randint(0, 255)
    b = randint(0, 255)
    turtle.pencolor(r,g,b)
    turtle.right(90)
    turtle.forward(100+x)
